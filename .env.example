# --- Client-side configuration (Vite) ---
# Supabase project identifier used by the frontend. Found in Supabase → Project Settings → General → Reference ID.
VITE_SUPABASE_PROJECT_ID=
# Public Supabase key for browser usage. Located in Supabase → Project Settings → API → anon public.
VITE_SUPABASE_PUBLISHABLE_KEY=
# Supabase REST URL from Project Settings → API.
VITE_SUPABASE_URL=
# Optional: Stripe Payment Link URL powering the "Rejoindre la Beta" CTA.
VITE_STRIPE_BETA_LINK=

# --- Server-side configuration (Vercel Functions) ---
# Absolute URL of your deployed site (e.g. https://example.vercel.app) used when building links in emails and webhooks.
PUBLIC_URL=
# Stripe secret API key for server-side operations. Create/retrieve under Stripe Dashboard → Developers → API keys.
STRIPE_SECRET_KEY=
# Stripe webhook signing secret for validating incoming webhooks. Copy from the Stripe Dashboard → Developers → Webhooks endpoint.
STRIPE_WEBHOOK_SECRET=
# Stripe Price IDs for each plan (Dashboard → Products → Price → API ID).
STRIPE_PRICE_ESSENTIAL=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=
# Supabase service role key for server-side Supabase calls. Find in Supabase → Project Settings → API → service_role.
SUPABASE_SERVICE_ROLE_KEY=
